// backend/routes/agent.js
const express = require('express');
const router = express.Router();

// Agente legado (Node) removido. Mantendo router vazio para evitar erros em require()
// Para restaurar: recupere do histórico Git ou consulte agent_files_separados.txt
module.exports = router;
